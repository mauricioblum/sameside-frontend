import React from 'react';
import Head from 'next/head';

const Form: React.FC = () => (
  <>
    <Head>
      <title>Simulador | Formulário</title>
    </Head>
    <h1>Preencha...</h1>
  </>
);

export default Form;
